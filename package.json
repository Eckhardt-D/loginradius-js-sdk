{
  "name": "loginradius-js-sdk",
  "version": "0.8.0",
  "description": "",
  "main": "build/src/index.js",
  "types": "build/src/index.d.ts",
  "files": [
    "build/src"
  ],
  "engines": {
    "node": "16.x.x"
  },
  "license": "MIT",
  "keywords": [
    "loginradius",
    "auth",
    "sdk"
  ],
  "author": {
    "name": "Eckhardt Dreyer",
    "email": "eckhardt@stillio.com"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint",
    "release:bump": "npm version bump && npm run lint && npm run compile && npm publish",
    "release:minor": "npm version minor && npm run lint && npm run compile && npm publish",
    "release:major": "npm version major && npm run lint && npm run compile && npm publish"
  },
  "devDependencies": {
    "gts": "^3.1.0",
    "typescript": "^4.0.3",
    "@types/node": "^14.11.2"
  }
}
